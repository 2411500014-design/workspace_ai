# Obsidian's per-machine UI state: open panes, window size, recent files.
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Deleted notes Obsidian keeps around.
.trash/

.DS_Store
Thumbs.db

# Interview recordings never enter the vault (it is committed to git).
*.m4a
*.mp3
*.wav
*.ogg
*.webm
*.aac
